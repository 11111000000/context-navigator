;; .magptrc — per-project settings for magpt.
;; Format: Emacs Lisp alist of (VARIABLE . VALUE) pairs.
;; These settings override user options (highest priority).

'(
  ;; Язык информационных сообщений (обзоры, пояснения, рационали)
  (magpt-info-language . "Русский")

  ;; Язык для генерируемых сообщений коммитов
  (magpt-commit-language . "English")


  (magpt-model . "gpt-4.1-mini"))